# Copy to .env. SQLite default runs out of the box.
DATABASE_URL="file:./dev.db"

# PostgreSQL alternative (see README "Switching to PostgreSQL"):
# DATABASE_URL="postgresql://dtot:dtot@localhost:5432/dtot?schema=public"
